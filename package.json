{
  "name": "sciter-webview-bridge",
  "version": "0.1.0",
  "description": "Sciter <-> WebView notification bridge (JS-only scope) + contract tests",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test test/"
  }
}
